{
  "name": "lexis-heartbeat",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker cron trigger que dispara los crons de Lexis cada 5 min (late fiable, sustituye a GitHub Actions).",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250901.0",
    "wrangler": "^3.80.0"
  }
}
